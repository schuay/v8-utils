[project]
name = "v8-mcp"
version = "0.1.0"
description = "MCP server exposing tools for V8 JavaScript engine developers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0",
]

[project.scripts]
v8-mcp = "server:mcp.run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
